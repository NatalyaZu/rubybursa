<h2>Петиция</h2>

<h3><%= @petition.title %></h3>

<p>
	<%= @petition.created_at.strftime("%d/%m/%Y %H:%M") %><br />
  <%= @petition.description %><br />
  <h4>Голосов:</h4><%=  %><br />
  <% if params[:my] = false %>
  	<%= button_to "Поддержать", vote_path(user_id: @petition.user.id, petition_id: @petition) %>
  <% end %>
</p>
